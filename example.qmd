---
title: "Quarto Typst Template"
subtitle: "BioMath GmbH"
author: "Dr. Paul Schmidt"
format: BioMathQuartoPDFTemplate-typst
---

Quarto extension for a template to generate a PDF with styling using Typst. 
You can find the underlying files here: https://github.com/SchmidtPaul/BioMathQuartoPDFTemplate

This template has been converted from LaTeX to Typst for faster compilation and modern typesetting.

# Header 1

## Header 2

Here is a vector whose output formatting is handled by Typst:

```{r}
letters
```

Here is some `inline code` and a code chunk:

```{r}
mod <- lm(weight ~ group, data = PlantGrowth)
anova(mod)
```

### Header 3

This subsection demonstrates the underlined header style.
